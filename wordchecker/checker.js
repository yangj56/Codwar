function checker(arr) {
    const checkedArr = []; // an array contains a tuple [0,1] === row 0, colum 1 which is letter N in this case
    return arr;
}

const wordArray = [
    ['I', 'L', 'A', 'W'],
    ['B', 'N', 'G', 'E'],
    ['I', 'U', 'A', 'O'],
    ['A', 'S', 'R', 'L'],
];

console.log(checker(wordArray));
